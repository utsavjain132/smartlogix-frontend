---
phase: 02-app-shells-layouts
plan: "04"
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - src/pages/Home.jsx
autonomous: true
requirements: [LAYT-03]

must_haves:
  truths:
    - "The landing page uses design tokens (bg-primary, text-primary, text-foreground, text-muted-foreground) with no hardcoded teal hex colors"
    - "The hero section renders the main heading and a Get Started CTA button"
    - "Feature cards render with design-token icon colors (className text-primary, not color prop hex)"
    - "The CTA gradient section uses from-primary to-primary/80 (not hardcoded teal gradient)"
    - "Footer social links use text-foreground hover:text-primary (not teal tokens)"
  artifacts:
    - path: "src/pages/Home.jsx"
      provides: "Landing page redesigned with design tokens"
      contains: "bg-primary"
  key_links:
    - from: "src/pages/Home.jsx"
      to: "src/index.css --primary token"
      via: "bg-primary, text-primary Tailwind classes using oklch(0.42 0.175 264)"
      pattern: "bg-primary"
---

<objective>
Replace all hardcoded teal colors in `Home.jsx` with design tokens and modernize the hero/features/CTA sections to match the Deep Blue SaaS palette established in Phase 1.

Purpose: Delivers LAYT-03 — modern SaaS landing page that visually aligns with the new Deep Blue theme instead of the legacy teal palette.
Output: Updated `Home.jsx` with zero hardcoded hex/teal color references.
</objective>

<execution_context>
@/home/utsav/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/utsav/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-app-shells-layouts/02-RESEARCH.md
@.planning/phases/02-app-shells-layouts/02-01-SUMMARY.md

<!-- Source file being modified -->
@src/pages/Home.jsx

<interfaces>
<!-- Design tokens from src/index.css (already active globally): -->
<!--   bg-primary         → oklch(0.42 0.175 264) — Deep Blue (was #00796B teal) -->
<!--   text-primary       → same oklch color for text -->
<!--   text-foreground    → high-contrast dark text (replaces text-teal-900) -->
<!--   text-muted-foreground → subdued text (replaces text-teal-700) -->
<!--   bg-background      → page surface (replaces bg-[#f4f7f6]) -->
<!--   border-border      → border color (replaces border-teal-100) -->
<!--   bg-card            → white card surface (replaces bg-white) -->
<!--   text-primary-foreground → text on bg-primary (white) -->

<!-- Lucide icons already imported in Home.jsx: Bot, MapPin, BadgeIndianRupee -->
<!-- Lucide icons use currentColor via SVG stroke — use className="text-primary" not color="#hex" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace all teal hardcodes in Home.jsx with design tokens</name>
  <files>src/pages/Home.jsx</files>
  <behavior>
    - Test: renders hero heading "Optimizing Freight Logistics..."
    - Test: renders Get Started CTA link
    - Test: no element in the rendered output has a class containing "#00796B", "#004D40", "teal-", or "bg-[#"
    - Test: renders three feature card headings
  </behavior>
  <action>
Rewrite `src/pages/Home.jsx` applying the token replacements from the research document (Pattern 5). Make these exact changes:

**Hero section:**
- Outer div: `text-teal-900` → `text-foreground`
- Hero heading `<h1>`: no class change needed (inherits foreground)
- Subheading `<p>`: `text-teal-700` → `text-muted-foreground`
- Get Started Button: `className="bg-[#00796B] hover:bg-[#004D40] text-white text-lg px-8 py-6 rounded-lg shadow-lg hover:-translate-y-1 transition-all"` → `size="lg" className="text-lg px-8 py-6 rounded-lg shadow-lg hover:-translate-y-1 transition-all"` (default variant = bg-primary automatically)

**Feature cards:**
- Each card outer div: `bg-white` → `bg-card`
- Feature card paragraph text: `text-teal-700` → `text-muted-foreground` (3 occurrences)
- Icon color prop: `color="#2563EB"` → `className="text-primary"` (3 occurrences: Bot, MapPin, BadgeIndianRupee)
  - `<Bot size={48} color="#2563EB" />` → `<Bot size={48} className="text-primary" />`
  - `<MapPin size={48} color="#2563EB" />` → `<MapPin size={48} className="text-primary" />`
  - `<BadgeIndianRupee size={48} color="#2563EB" />` → `<BadgeIndianRupee size={48} className="text-primary" />`

**CTA section:**
- Outer div: `bg-gradient-to-br from-[#00796B] to-[#004D40]` → `bg-gradient-to-br from-primary to-primary/80`
- Subtext: `text-[#B2DFDB]` → `text-primary-foreground/80`
- "Join as Trucker" Button: `className="bg-white text-[#00796B] hover:bg-[#E0F2F1] text-lg px-8 py-6 rounded-lg border border-[#00796B] shadow-lg hover:-translate-y-1 transition-all"` → `variant="secondary" className="text-lg px-8 py-6 rounded-lg shadow-lg hover:-translate-y-1 transition-all"`
- "Join as Business Owner" Button: `className="bg-[#00796B] hover:bg-[#004D40] text-white text-lg px-8 py-6 rounded-lg border border-white/20 shadow-lg hover:-translate-y-1 transition-all"` → `className="text-lg px-8 py-6 rounded-lg border border-primary-foreground/20 shadow-lg hover:-translate-y-1 transition-all"` (default variant)

**Footer:**
- `border-teal-100` → `border-border`
- Social link hover: `text-teal-900 hover:text-[#00796B]` → `text-muted-foreground hover:text-primary`

**Hero illustration placeholder:** Leave the empty `<div className="hero-illustration">` as-is.

After all replacements, do a final check: run `grep -n "teal\|#00796\|#004D40\|#B2DFDB\|#E0F2F1\|#2563EB\|color=" src/pages/Home.jsx` — the output must be empty (zero matches).
  </action>
  <verify>
    <automated>npx vitest run tests/Home.test.jsx</automated>
  </verify>
  <done>
  - All 4 Home tests pass: hero heading renders, Get Started link renders, no teal/hardcoded-hex class found, all 3 feature card headings render.
  - `grep "teal\|#00796\|#004D40\|color=" src/pages/Home.jsx` returns no results.
  - `npx vitest run tests/` — no regressions.
  </done>
</task>

</tasks>

<verification>
```bash
npx vitest run tests/Home.test.jsx
```
Manual scan for remaining hardcodes:
```bash
grep -n "teal\|#00796\|#004D40\|#B2DFDB\|color=" src/pages/Home.jsx
```
Full suite regression:
```bash
npx vitest run tests/
```
</verification>

<success_criteria>
- `src/pages/Home.jsx` has zero occurrences of: `teal-`, `#00796B`, `#004D40`, `#B2DFDB`, `#E0F2F1`, `#2563EB`, `color="` (Lucide color prop)
- All Lucide icons use `className="text-primary"` for color
- CTA gradient section uses `from-primary to-primary/80`
- All 4 Home tests GREEN
- `npx vitest run tests/` — all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shells-layouts/02-04-SUMMARY.md`
</output>
