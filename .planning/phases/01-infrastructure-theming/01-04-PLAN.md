---
phase: 01-infrastructure-theming
plan: 04
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/pages/LoginPage.jsx
  - src/pages/SignupPage.jsx
  - src/pages/LoginPage.css
  - src/pages/SignupPage.css
  - tests/LoginPage.test.jsx
autonomous: true
requirements:
  - INFR-01

must_haves:
  truths:
    - "Login and Signup flows are styled with modern Shadcn components"
    - "Authentication pages have no legacy CSS dependencies"
  artifacts:
    - path: "src/pages/LoginPage.jsx"
      provides: "Migrated login form using utility classes"
    - path: "src/pages/SignupPage.jsx"
      provides: "Migrated signup form using utility classes"
  key_links:
    - from: "src/pages/LoginPage.jsx"
      to: "@/components/ui/input"
      via: "form field rendering"
---

<objective>
Modernize Authentication forms using Shadcn primitives.

Purpose: Strip legacy CSS from authentication pages, converting them into centered SaaS Card components with accessible inputs and robust typography.
Output: Login and Signup flows using pure Tailwind classes and Shadcn primitives, free of `.css` files.
</objective>

<execution_context>
@/home/utsav/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/utsav/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Login Flow</name>
  <files>src/pages/LoginPage.jsx, src/pages/LoginPage.css</files>
  <action>Migrate `LoginPage.jsx` to utilize Shadcn `Card`, `Input`, `Label`, and `Button` components. Center the entire form cleanly on the screen using Tailwind flex box and min-height classes. Completely delete `LoginPage.css` from the repository.</action>
  <verify>
    <automated>test ! -f src/pages/LoginPage.css</automated>
  </verify>
  <done>Login page rebuilt with Shadcn components, centered via utilities, and CSS deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Signup Flow</name>
  <files>src/pages/SignupPage.jsx, src/pages/SignupPage.css</files>
  <action>Migrate `SignupPage.jsx` using Shadcn `Card`, `Input`, `Label`, and `Button` components. Since signup forms are larger, build a clean grid or stacked layout for the input groups using Tailwind utilities to ensure spacing is tight. Completely delete `SignupPage.css` from the repository.</action>
  <verify>
    <automated>test ! -f src/pages/SignupPage.css</automated>
  </verify>
  <done>Signup page rebuilt with Shadcn components, structured via utilities, and CSS deleted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Auth rendering test</name>
  <files>tests/LoginPage.test.jsx</files>
  <behavior>
    - Test 1: Given the LoginPage is rendered, the email and password inputs should be visible and accessible.
  </behavior>
  <action>Create a basic component test `tests/LoginPage.test.jsx` verifying that the Login page renders correctly and the newly integrated Shadcn form fields (email, password, button) mount without failing. Run vitest to confirm the integration.</action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Authentication flow component smoke test is written and passing.</done>
</task>

</tasks>

<verification>
Ensure the login and signup routes render perfectly centered card components, inputs accept text correctly, and submit buttons function without errors.
</verification>

<success_criteria>
- Zero usage of `LoginPage.css` or `SignupPage.css`.
- Forms strictly use imported Shadcn UI base primitives.
- Input elements match Shadcn's visual defaults, replacing legacy focus borders.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-theming/01-04-SUMMARY.md`
</output>