---
phase: 01-infrastructure-theming
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - components.json
  - src/index.css
  - index.html
  - src/lib/utils.js
  - src/components/ui/button.jsx
  - src/components/ui/input.jsx
  - src/components/ui/label.jsx
  - src/components/ui/card.jsx
autonomous: true
requirements:
  - INFR-03
  - INFR-04
  - INFR-05

must_haves:
  truths:
    - "Application defines a SaaS Deep Blue theme via inline Tailwind directives"
    - "Inter typography is available globally via Google Fonts"
    - "Shadcn CLI is configured and core component primitives exist"
  artifacts:
    - path: "src/index.css"
      provides: "Global Tailwind configuration and standard base resets"
    - path: "index.html"
      provides: "Inter font CDN link in document head"
    - path: "components.json"
      provides: "Shadcn initialization configuration"
  key_links:
    - from: "src/index.css"
      to: "@theme inline"
      via: "CSS configuration"
---

<objective>
Initialize Shadcn components and configure the global enterprise theme.

Purpose: Provide a strong component basis built on Radix primitives and Tailwind v4, ensuring future UI tasks have native Shadcn elements to use. Set a high-contrast SaaS typography and palette.
Output: Integrated Shadcn library, Deep Blue design tokens, and Inter fonts configured.
</objective>

<execution_context>
@/home/utsav/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/utsav/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Shadcn CLI and primitives</name>
  <files>components.json, src/lib/utils.js, src/components/ui/button.jsx, src/components/ui/input.jsx, src/components/ui/label.jsx, src/components/ui/card.jsx</files>
  <action>Initialize Shadcn UI automatically using `npx shadcn@latest init -y -d`. After initialization, add fundamental UI primitives by running `npx shadcn@latest add button input label card`. Verify `components.json` correctly maps the `@` alias for utilities and components.</action>
  <verify>
    <automated>ls src/components/ui/button.jsx</automated>
  </verify>
  <done>Shadcn CLI integrated and base primitive files generated.</done>
</task>

<task type="auto">
  <name>Task 2: Configure global theme and typography</name>
  <files>src/index.css, index.html</files>
  <action>Update `index.html` to load the 'Inter' font from Google Fonts. Open `src/index.css` (which shadcn modified during init) and customize the Tailwind `@theme` block. Map `--color-primary` and related palette variables to a Deep Blue / Slate SaaS theme. Set the `--font-sans` variable to `"Inter", sans-serif`. Ensure basic `@layer base` styles (like resetting the body background) remain intact.</action>
  <verify>
    <automated>grep -q "Inter" index.html</automated>
  </verify>
  <done>Deep blue palette and Inter typography globally applied via Tailwind utilities.</done>
</task>

</tasks>

<verification>
Start the dev server and verify the browser console has no Shadcn alias or Google Font 404 errors.
</verification>

<success_criteria>
- The project is fully uncoupled from legacy custom themes.
- Reusable, accessible component files exist in `src/components/ui`.
- The Deep Blue theme renders correctly across elements.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-theming/01-02-SUMMARY.md`
</output>