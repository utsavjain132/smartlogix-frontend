---
phase: 01-infrastructure-theming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.js
  - jsconfig.json
  - tests/setup.js
  - tests/App.test.jsx
autonomous: true
requirements:
  - INFR-02

must_haves:
  truths:
    - "Test framework can run smoke tests against React components"
    - "Vite resolves `@/` paths to the `src/` directory"
    - "Tailwind CSS v4 is cleanly integrated without legacy PostCSS tooling"
  artifacts:
    - path: "vite.config.js"
      provides: "Vite config with testing and alias support"
    - path: "jsconfig.json"
      provides: "IDE path alias resolution"
    - path: "tests/App.test.jsx"
      provides: "Basic mounting test for the application shell"
  key_links:
    - from: "vite.config.js"
      to: "@tailwindcss/vite"
      via: "plugins array"
---

<objective>
Establish build and testing infrastructure to safely transition styling frameworks.

Purpose: Prevent regressions during massive CSS purges by having automated smoke tests, and properly configure Vite for Tailwind v4 and Shadcn imports.
Output: Tested application shell, resolved aliases, and optimized build config.
</objective>

<execution_context>
@/home/utsav/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/utsav/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup testing infrastructure</name>
  <files>package.json, vite.config.js, tests/setup.js</files>
  <action>Install `vitest`, `jsdom`, `@testing-library/react`, and `@testing-library/jest-dom`. Update `vite.config.js` to include the `test` property configured for jsdom environment and loading `tests/setup.js`. Create `tests/setup.js` importing `@testing-library/jest-dom`.</action>
  <verify>
    <automated>npx vitest --version</automated>
  </verify>
  <done>Vitest framework installed and configured with React Testing Library.</done>
</task>

<task type="auto">
  <name>Task 2: Configure path aliases and Tailwind plugin</name>
  <files>jsconfig.json, vite.config.js, package.json</files>
  <action>Create `jsconfig.json` setting `compilerOptions.baseUrl` to "." and `compilerOptions.paths` mapping `@/*` to `./src/*`. Update `vite.config.js` to add a `resolve.alias` mapping `@` to the `src` folder (use `path.resolve(import.meta.dirname, "./src")`). Remove `postcss`, `autoprefixer`, and `@tailwindcss/postcss` from `package.json` devDependencies since we exclusively use `@tailwindcss/vite`.</action>
  <verify>
    <automated>cat jsconfig.json | grep -q compilerOptions</automated>
  </verify>
  <done>Path aliases configured for Shadcn, redundant PostCSS tools removed.</done>
</task>

<task type="auto">
  <name>Task 3: Add application smoke test</name>
  <files>tests/App.test.jsx</files>
  <action>Create a basic test `tests/App.test.jsx` that mounts the main `App` component within Vitest. This ensures our alias and Vite configurations did not fundamentally break rendering.</action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Smoke test passes and correctly renders the application shell.</done>
</task>

</tasks>

<verification>
Check that `npx vitest run` executes cleanly, and `npm run build` outputs successfully with Tailwind applied natively through Vite.
</verification>

<success_criteria>
- PostCSS dependencies are entirely removed.
- Tests execute automatically via CLI.
- Aliases resolve cleanly without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-theming/01-01-SUMMARY.md`
</output>