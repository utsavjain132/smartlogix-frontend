---
phase: 01-infrastructure-theming
plan: 06
type: execute
wave: 4
depends_on:
  - "01-03"
  - "01-04"
  - "01-05"
files_modified:
  - src/pages/TruckerDashboard.jsx
  - src/pages/TruckerDashboard.css
autonomous: false
requirements:
  - INFR-01

must_haves:
  truths:
    - "Trucker dashboard renders basic structure via Tailwind utilities"
    - "User can visually confirm the SaaS transformation of the entire application"
  artifacts:
    - path: "src/pages/TruckerDashboard.jsx"
      provides: "Migrated trucker view"
  key_links:
    - from: "src/pages/TruckerDashboard.jsx"
      to: "Tailwind"
      via: "inline class names"
---

<objective>
Migrate the Trucker Dashboard and conduct the final Phase 1 checkpoint verification.

Purpose: Eliminate the last remaining piece of legacy CSS and ensure the entire platform visually functions smoothly under the new Tailwind/Shadcn regime before moving onto layouts in Phase 2.
Output: Tailwind-driven Trucker Dashboard and an app entirely free of legacy CSS.
</objective>

<execution_context>
@/home/utsav/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/utsav/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Trucker Dashboard</name>
  <files>src/pages/TruckerDashboard.jsx, src/pages/TruckerDashboard.css</files>
  <action>Remove `TruckerDashboard.css` entirely from the repository and delete its import from `TruckerDashboard.jsx`. Apply fundamental mobile-first Tailwind utility classes (flex, gap, padding, margin, text-sizes) onto the primary markup tags. The objective is to prevent total layout collapse by maintaining structural integrity through Tailwind; specific mobile cards and badges are handled in Phase 3.</action>
  <verify>
    <automated>test ! -f src/pages/TruckerDashboard.css</automated>
  </verify>
  <done>Trucker dashboard CSS stripped and baseline Tailwind layout applied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 1 transformation</name>
  <files>None</files>
  <action>Pause for human verification of the full phase UI transformation.</action>
  <what-built>All legacy CSS has been deleted across the platform. Tailwind v4 and Shadcn components have been applied to core structural elements. The Inter typography scale and a high-trust Deep Blue SaaS palette are globally active.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the local preview.
    2. Click through the public Home, Login, and Signup pages. Ensure forms look like modern SaaS cards.
    3. Login as a Trucker, Business, and Admin user sequentially to view their respective dashboards.
    4. Confirm that despite deleting all .css files, the application layouts remain structurally organized and standard inputs/buttons function flawlessly with a Deep Blue aesthetic.
  </how-to-verify>
  <verify>
    <automated>echo "Waiting for human verification"</automated>
  </verify>
  <resume-signal>Type "approved" if the application visually holds together without structural collapse and the colors/fonts reflect the modern SaaS theme.</resume-signal>
  <done>User confirmed complete legacy CSS removal.</done>
</task>

</tasks>

<verification>
Zero instances of `*.css` imports (other than `index.css` or third-party packages) remain anywhere within the React codebase.
</verification>

<success_criteria>
- The phase-wide transition to Tailwind v4 is completed.
- User approves visual layout parity given the removal of legacy stylesheets.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-theming/01-06-SUMMARY.md`
</output>