---
phase: 01-infrastructure-theming
plan: 05
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/pages/BusinessDashboard.jsx
  - src/pages/AdminDashboard.jsx
  - src/pages/BusinessDashboard.css
  - src/pages/AdminDashboard.css
autonomous: true
requirements:
  - INFR-01

must_haves:
  truths:
    - "Business and Admin dashboards render basic structure via Tailwind utilities"
    - "Legacy CSS files are completely stripped from the dashboards"
  artifacts:
    - path: "src/pages/BusinessDashboard.jsx"
      provides: "Business view sans-CSS"
    - path: "src/pages/AdminDashboard.jsx"
      provides: "Admin view sans-CSS"
  key_links:
    - from: "src/pages/BusinessDashboard.jsx"
      to: "Tailwind"
      via: "inline class names"
---

<objective>
Migrate complex dashboard views strictly to inline Tailwind utilities.

Purpose: Eliminate legacy CSS from dense data dashboards to set a clean structural foundation before Phase 3's Data Tables overhaul.
Output: Business and Admin dashboards rendered without `.css` files, preserving basic layout via Tailwind classes.
</objective>

<execution_context>
@/home/utsav/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/utsav/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Business Dashboard</name>
  <files>src/pages/BusinessDashboard.jsx, src/pages/BusinessDashboard.css</files>
  <action>Remove `BusinessDashboard.css` from the repository entirely. Remove its import from `BusinessDashboard.jsx` and apply fundamental Tailwind flex, grid, margin, padding, and text utility classes directly onto the markup to prevent the layout from visually collapsing. The goal here is solely structural preservation using utilities; detailed component redesigns (tables, modals) happen in later phases.</action>
  <verify>
    <automated>test ! -f src/pages/BusinessDashboard.css</automated>
  </verify>
  <done>Business dashboard CSS stripped and baseline Tailwind layout applied.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Admin Dashboard</name>
  <files>src/pages/AdminDashboard.jsx, src/pages/AdminDashboard.css</files>
  <action>Remove `AdminDashboard.css` from the repository entirely. Remove its import from `AdminDashboard.jsx` and apply fundamental Tailwind structural utility classes (flex, gap, padding, text-sizes) to the container elements, headers, and standard layout blocks to ensure the dashboard layout holds together. The goal here is purely structural preservation via utilities.</action>
  <verify>
    <automated>test ! -f src/pages/AdminDashboard.css</automated>
  </verify>
  <done>Admin dashboard CSS stripped and baseline Tailwind layout applied.</done>
</task>

</tasks>

<verification>
Start the dev server, login with Business and Admin mock credentials, and visually confirm the dashboards render in a structured manner despite the massive CSS purge.
</verification>

<success_criteria>
- Zero custom `.css` dependencies exist for Business or Admin dashboards.
- Layout remains visually organized and not structurally broken.
- No React "CSS module not found" runtime errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-theming/01-05-SUMMARY.md`
</output>