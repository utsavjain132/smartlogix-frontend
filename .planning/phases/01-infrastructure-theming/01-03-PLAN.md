---
phase: 01-infrastructure-theming
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/App.jsx
  - src/components/Navbar.jsx
  - src/pages/Home.jsx
  - src/pages/ContactPage.jsx
  - src/App.css
  - src/components/Navbar.css
  - src/pages/Home.css
  - src/pages/ContactPage.css
autonomous: true
requirements:
  - INFR-01

must_haves:
  truths:
    - "Core app shell, navigation, and landing pages render without their legacy CSS files"
    - "Navbar integrates Shadcn buttons and Tailwind utilities for its layout"
  artifacts:
    - path: "src/App.jsx"
      provides: "Application shell without legacy styles"
    - path: "src/components/Navbar.jsx"
      provides: "Modernized navigation bar"
  key_links:
    - from: "src/components/Navbar.jsx"
      to: "@/components/ui/button"
      via: "import and component usage"
---

<objective>
Migrate the Core App Shell, Navbar, and Landing Page to modern Tailwind utilities.

Purpose: Strip legacy CSS globally starting from the outermost layout components and public-facing routes, validating the Shadcn and Tailwind configurations.
Output: Tailwind-driven App and Navbar, completely deleted legacy `.css` files.
</objective>

<execution_context>
@/home/utsav/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/utsav/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate App Shell and Navbar</name>
  <files>src/App.jsx, src/App.css, src/components/Navbar.jsx, src/components/Navbar.css</files>
  <action>Replace `Navbar.css` and `App.css` usages with Shadcn `Button` components and Tailwind utility classes directly within `App.jsx` and `Navbar.jsx`. Use Tailwind grid/flex utilities to match the original layout. Completely delete `Navbar.css` and `App.css` from the file system.</action>
  <verify>
    <automated>test ! -f src/App.css && test ! -f src/components/Navbar.css</automated>
  </verify>
  <done>App and Navbar rendered exclusively with Tailwind utilities and Shadcn primitives.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Landing and Contact Pages</name>
  <files>src/pages/Home.jsx, src/pages/Home.css, src/pages/ContactPage.jsx, src/pages/ContactPage.css</files>
  <action>Replace `Home.css` and `ContactPage.css` usages with Shadcn components and equivalent Tailwind utility classes in `Home.jsx` and `ContactPage.jsx`. Maintain the clean public-facing layout using standard padding and margin scales. Completely delete the legacy CSS files.</action>
  <verify>
    <automated>test ! -f src/pages/Home.css</automated>
  </verify>
  <done>Public pages migrated safely and legacy CSS permanently deleted.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify core routing integrity</name>
  <files>tests/App.test.jsx</files>
  <action>Run the `tests/App.test.jsx` smoke test to ensure that the removal of these core CSS imports did not cause any missing module import errors during runtime or component rendering issues.</action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Tests pass, confirming no broken CSS import references remain.</done>
</task>

</tasks>

<verification>
Start the application and verify the home page renders cleanly, and the navbar resizes correctly on mobile and desktop without errors.
</verification>

<success_criteria>
- Zero usage of `App.css` or `Navbar.css` in the project.
- Visual parity with legacy layout using pure utility classes.
- Shadcn buttons used for primary CTAs in the Navbar and Home page.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-theming/01-03-SUMMARY.md`
</output>